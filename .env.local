# Backend API base URL used by API proxy routes
# IMPORTANT: Set this to your real backend, NOT the Next.js dev server port 3000
BACKEND_URL=http://localhost:8686

# Expose the same base URL to the client for building absolute image URLs
NEXT_PUBLIC_BACKEND_URL=http://localhost:8686
